// This whole file only exists to have a main function conflict,
// which should not be linked against. This entry point does *nothing*.

#include <cstdlib>

int main(int, char**) {
	return EXIT_SUCCESS;
}
